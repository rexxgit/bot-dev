{
  "name": "bot-dev",
  "version": "1.0.0",
  "type": "module",
  "description": "Omni Brand Intelligence Bot with multi-source scraping and AI-powered search",
  "main": "index.html",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp index.html public/ && cp -r api public/ && cp -r data.json public/ || true",
    "start": "vercel start",
    "deploy": "vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "vercel": "^37.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rexxgit/bot-dev"
  },
  "keywords": [
    "ai",
    "rag",
    "search",
    "scraping",
    "techcrunch"
  ],
  "author": "rexxgit",
  "license": "MIT"
}
